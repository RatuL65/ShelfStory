name: Build and Release Flutter APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # Step 3: Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      # Step 4: Decode google-services.json (ADDED - Must come before pub get)
      - name: Decode google-services.json
        run: |
          echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | base64 --decode > android/app/google-services.json

      # Step 5: Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # Step 6: Generate Hive files
      - name: Generate Hive adapter files
        run: flutter packages pub run build_runner build --delete-conflicting-outputs

      # Step 7: Build APK
      - name: Build APK
        run: flutter build apk --release

      # Step 8: Get version from pubspec.yaml
      - name: Get version
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2 | tr -d '\r')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # Step 9: Create Release and Upload APK
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/app/outputs/flutter-apk/app-release.apk"
          tag: v${{ steps.version.outputs.version }}-build-${{ github.run_number }}
          name: ShelfStory v${{ steps.version.outputs.version }} (Build ${{ github.run_number }})
          body: |
            ## 📚 ShelfStory - Phone Authentication & Cloud Ready
            
            **Version:** ${{ steps.version.outputs.version }}
            **Build Number:** ${{ github.run_number }}
            **Commit:** ${{ github.sha }}
            
            ### ✨ What's New in This Version
            - ✅ Firebase Phone Authentication enabled
            - ✅ Cloud-ready infrastructure with Firestore
            - ✅ Updated Android SDK to 36
            - ✅ Secure OTP-based login
            - 🔧 Bug fixes and improvements
            
            ### 📥 Download & Install
            1. Download `app-release.apk` below
            2. Enable "Install from Unknown Sources" in phone settings
            3. Open APK and install
            4. Sign in with your phone number
            
            ### 🎯 Features
            - Track books with vintage UI
            - Barcode scanning for ISBN
            - Reading statistics and streaks
            - Phone number authentication
            - Export library as CSV
            
            ### 🔧 Built with GitHub Actions
            Automatically built and deployed from the cloud!
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          makeLatest: true
