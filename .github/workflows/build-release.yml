name: Build and Release Flutter APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # Step 3: Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      # Step 4: Create .android directory
      - name: Create Android debug directory
        run: mkdir -p /home/runner/.android

      # Step 5: Decode debug keystore
      - name: Decode debug keystore
        run: |
          echo "${{ secrets.DEBUG_KEYSTORE }}" | base64 --decode > /home/runner/.android/debug.keystore

      # Step 5.5: Print keystore SHA-1 (DEBUG)
      - name: Print keystore SHA-1
        run: |
          keytool -list -v -keystore /home/runner/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android

      # Step 6: Decode google-services.json
      - name: Decode google-services.json
        run: |
          echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | base64 --decode > android/app/google-services.json

      # Step 7: Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # Step 8: Generate Hive files
      - name: Generate Hive adapter files
        run: flutter packages pub run build_runner build --delete-conflicting-outputs

      # Step 9: Build APK
      - name: Build APK
        run: flutter build apk --release

      # Step 10: Get version from pubspec.yaml
      - name: Get version
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2 | tr -d '\r')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # Step 11: Create Release and Upload APK
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "build/app/outputs/flutter-apk/app-release.apk"
          tag: v${{ steps.version.outputs.version }}-build-${{ github.run_number }}
          name: ShelfStory v${{ steps.version.outputs.version }} (Build ${{ github.run_number }})
          body: |
            ## 📚 ShelfStory - Google Sign-In & Cloud Ready
            
            **Version:** ${{ steps.version.outputs.version }}
            **Build Number:** ${{ github.run_number }}
            **Commit:** ${{ github.sha }}
            
            ### ✨ What's New in This Version
            - ✅ Google Sign-In authentication enabled
            - ✅ Cloud-ready infrastructure with Firestore
            - ✅ Updated Android SDK to 36
            - ✅ Secure authentication flow
            - 🔧 Bug fixes and improvements
            
            ### 📥 Download & Install
            1. Download `app-release.apk` below
            2. Enable "Install from Unknown Sources" in phone settings
            3. Open APK and install
            4. Sign in with your Google account
            
            ### 🎯 Features
            - Track books with vintage UI
            - Barcode scanning for ISBN
            - Reading statistics and streaks
            - Google authentication
            - Export library as CSV
            
            ### 🔧 Built with GitHub Actions
            Automatically built and deployed from the cloud!
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          makeLatest: true
